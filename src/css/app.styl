@keyframes pulse {
	0% {
		transform: scale(0.95);
	}

	70% {
		transform: scale(1);
	}

	100% {
		transform: scale(0.95);
	}
}
.pulse {
  animation: pulse 2s infinite;
	-webkit-animation: pulse 2s infinite;
	-moz-animation: pulse 2s infinite;
	-o-animation: pulse 2s infinite;
  transform-origin: center center;
}

@keyframes bounce {
  from, 20%, 53%, 80%, to {
    animation-timing-function: cubic-bezier(0.215, 0.610, 0.355, 1.000);
    transform: translate3d(0,0,0);
  }

  40%, 43% {
    animation-timing-function: cubic-bezier(0.755, 0.050, 0.855, 0.060);
    transform: translate3d(0, -30px, 0);
  }

  70% {
    animation-timing-function: cubic-bezier(0.755, 0.050, 0.855, 0.060);
    transform: translate3d(0, -15px, 0);
  }

  90% {
    transform: translate3d(0,-4px,0);
  }
}

.bounce {
  animation: bounce 2s infinite;
	-webkit-animation: bounce 2s infinite;
	-moz-animation: bounce 2s infinite;
	-o-animation: bounce 2s infinite;
  transform-origin: center bottom;
}

.hover-rotate{ transition: all 0.3s ease-in-out 0s; }

.hover-rotate:hover{
    transform: rotate(-15deg);
    transition: all 0.3s ease-in-out 0s;
}

.rounded-borders-right{
  border-top-right-radius: 4px;
  border-bottom-right-radius: 4px;
}

.rounded-borders-left{
  border-top-left-radius: 4px;
  border-bottom-left-radius: 4px;
}

.rounded-borders-bottom{
  border-bottom-left-radius: 4px;
  border-bottom-right-radius: 4px;
}


.round{
  border-radius: 50%;
}

.display-none{
  display:none;
}

.q-pr-xxl{
  padding-right:80px;
}

.q-input-bg{
  background-color: white;
}

.cursor-default{
  cursor: default !important;
}

input[type=number]::-webkit-outer-spin-button,
input[type=number]::-webkit-inner-spin-button {
    -webkit-appearance: none;
    margin: 0;
}

input[type=number] {
    -moz-appearance:textfield;
}
textarea {resize: none!important}

.border-primary{
  border: 1px solid var(--q-color-primary) ;
}
.border-primary-2{
  border: 2px solid var(--q-color-primary);
}

.bg-transparent{
  background-color: transparent;
}

.center-page-content{
  max-width:1200px;
  margin:0 auto;
}

.constrain-page-width{
  max-width:1120px;
}

 /* unvisited link */
.text-link {
  color: var(--q-color-primary);
  text-decoration: none;
  transition: color 0.5s ease;
}

/* visited link */
.text-link:visited {
  color: var(--q-color-primary);
}

/* mouse over link */
.text-link:hover {
  color: grey;
}

/* selected link */
.text-link:active {
  color: var(--q-color-primary);
}

.text-link2, .text-link2:visited{
  text-decoration: none;
  transition: color 0.5s ease;
  color: var(--q-color-white);
}
.text-link2:hover, .text-link2:active  {
  color: var(--q-color-primary);
}

p {
  text-align: justify;
}

.info-box{
  padding:20px;
  border: 2px solid $primary;
  border-radius: 3px;
  background-color: #D0DCFF;
  font-size:12px;
  text-align: justify;
}

.warning-box{
  padding:20px;
  border: 2px solid $negative;
  border-radius: 3px;
  background-color: #EFDBE3;
  font-size:12px;
  text-align: justify;
}

.q-router-link--exact-active{
  // background: var(--q-color-primary) !important;
  background:var(--q-color-primary) !important;
  color: white!important;
}

.q-router-link--exact-active .q-item__label--caption{
  color: grey!important;
}

.primary-hover-list .q-hoverable:hover:not(.q-router-link--exact-active) > .q-focus-helper{
	  background: var(--q-color-primary) !important;
}

.json-pretty {
    /* padding-left: 30px;
    padding-right: 30px; */
}
.json-selected {
    background-color: rgba(180, 180, 180, 0.19999999999999996);
}

.json-string {
    color: #6caedd;
}

.json-key {
    color: #ec5f67;
}

.json-boolean {
    color: #99c794;
}

.json-number {
    color: #99c794;
}




.body--light {
  background: #FAFAFA;

}

// .body--dark {
//   background-color: #0C0C0C !important;
// }

.q-markdown--link{
  color: var(--q-color-primary)}
  
.q-markdown--link:hover{
  color: grey
}

blockquote.q-markdown--note{
  border-color: var(--q-color-primary) !important;
}
    

